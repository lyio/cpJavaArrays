
public class DogArray {
    public static Dog[] getDogs () {
        final Dog[] dogs = {new Dog(), new Dog(), new Dog()};

        // the other way to initialize the array
        // final Dog[] dogs;
        // dogs = new Dog[5];

        return dogs;
    }
}
